services:
  - type: web
    name: bd-brutal-bomber
    env: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node bd_brutal_bomber.js
    envVars:
      - key: PORT
        value: 3000
    healthCheckPath: /health
    autoDeploy: true